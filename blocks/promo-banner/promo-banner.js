/**
 * Promo Banner Block
 * Full-width rounded-corner colored banner with heading, body text, and a
 * pill-shaped CTA button — matches the "Hi, I'm Joy!" and "Supercharge your
 * shop" (OnePass) promo banners on the real Kmart homepage.
 *
 * Authoring model: single row, single cell containing a heading, a
 * paragraph, and a link (rendered as the pill CTA).
 */
export default function decorate(block) {
  // default content renders as-is; CSS handles the visual treatment
}
