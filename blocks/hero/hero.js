/**
 * Hero Block
 * Simple passthrough decorator — content (heading, description, CTA,
 * background picture) is authored directly and rendered as default content
 * inside the block wrapper. No structural changes needed beyond marking
 * the block as loaded.
 */
export default function decorate(block) {
  // no-op: default content renders as authored
}
